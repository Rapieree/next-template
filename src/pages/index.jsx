import Layout from "../components/layout/layout";

const Home = () => {
  return (
    <Layout title={`Main page`} >
      <div className={`container`}>
        <h2>Test page</h2>
      </div>
    </Layout>
  );
};

export default Home;
